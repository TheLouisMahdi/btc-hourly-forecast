# دستور نصب پچ v2

- داشبورد را ببند.
- از `data` و `artifacts` بکاپ بگیر.
- محتویات پچ را روی پوشهٔ پروژه قبلی جایگزین کن.
- `setup.bat` را اجرا کن.
- سپس `start_retrain.bat` را اجرا کن.

مدل قبلی حذف اجباری نمی‌شود، اما توسط v2 رد خواهد شد. پس از Train موفق، `latest.joblib` با مدل جدید جایگزین می‌شود. دیتابیس قبلی نیز بدون حذف سیگنال‌های گذشته Migration می‌شود.
