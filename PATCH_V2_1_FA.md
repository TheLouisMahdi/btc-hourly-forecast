# پچ v2.1 — Meta-Label Event Trader

این پچ سه خطای مهم نسخه 2.0 را اصلاح می‌کند:

1. مدل جهت عمومی روی همه کندل‌ها آموزش می‌دید، ولی معامله فقط روی رویدادها انجام می‌شد.
2. برچسب Tradeability فقط بزرگی حرکت را می‌سنجید و جهت رویداد را در نظر نمی‌گرفت.
3. برچسب‌های Continuation و Triple-Barrier ساخته می‌شدند، اما در هدف معاملاتی مدل استفاده نمی‌شدند.

در v2.1:

- پیش‌بینی عمومی UP/DOWN فقط برای نمایش داشبورد باقی می‌ماند.
- مدل رویداد فقط روی رویدادهای مستقل آموزش می‌بیند.
- جهت معامله را رویداد اصلی تعیین می‌کند.
- مدل فقط احتمال ادامه رویداد و سودده‌بودن آن پس از هزینه را تخمین می‌زند.
- بازده رگرسیون رویداد، بازده هم‌جهت با رویداد است.
- Qualification دیگر با AUC عمومی UP/DOWN معامله را رد یا قبول نمی‌کند؛ معیارهای رویدادی ملاک‌اند.
- Calibration دیگر بعد از بازآموزی مدل پایه روی کل داده نامعتبر نمی‌شود.

## نصب

1. داشبورد و Live Engine را ببندید.
2. از پوشه‌های `data` و `artifacts` نسخه پشتیبان بگیرید.
3. فایل‌های این پچ را روی پوشه پروژه v2 جایگزین کنید.
4. اجرا کنید:

```bat
setup.bat
start_retrain.bat
```

مدل قبلی عمداً ناسازگار شناخته می‌شود. شناسه مدل جدید با `regime-meta-hourly-` شروع می‌شود و schema آن 3 است.
